<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/incidentsbar_row"
    android:layout_width="match_parent"
    android:layout_height="70dp"
    app:layout_constraintTop_toTopOf="parent">

    <ImageButton
        android:id="@+id/incidents_bar_add_btn"
        android:layout_width="35dp"
        android:layout_height="32dp"
        android:layout_marginStart="30dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/add_icon" />

    <ImageView
        android:id="@+id/incidents_bar_title_icon"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginEnd="20dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/incidents_bar_add_btn"
        app:srcCompat="@drawable/ic_notifications_black_24dp" />

    <TextView
        android:id="@+id/incidents_bar_title"
        android:layout_width="65dp"
        android:layout_height="0dp"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="-5dp"
        android:text="@string/title_incidents"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@+id/incidents_bar_title_icon"
        app:layout_constraintEnd_toStartOf="@+id/incidents_bar_title_icon"
        app:layout_constraintTop_toTopOf="@+id/incidents_bar_title_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>